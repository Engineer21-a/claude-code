# Runtime + dev dependencies, pinned to the versions this project was verified
# against. `pip install -e .[dev]` is the preferred install; this file exists for
# reproducible CI / fresh environments.
numpy==2.4.6
scipy==1.17.1
sympy==1.14.0
matplotlib==3.11.0
numba==0.65.1
pytest==9.1.0
ruff==0.15.17
mypy==2.1.0
