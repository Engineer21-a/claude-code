# Runtime + dev dependencies, pinned to the exact versions this project was
# verified against. CI installs via `pip install -e ".[dev]"` (resolving the
# ranges in pyproject.toml); this lockfile is a convenience for reproducing the
# verified environment in a fresh checkout: `pip install -r requirements.txt`.
numpy==2.4.6
scipy==1.17.1
sympy==1.14.0
matplotlib==3.11.0
numba==0.65.1
pytest==9.1.0
ruff==0.15.17
mypy==2.1.0
